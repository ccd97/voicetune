# Step 1: Pre-processing
av==17.0.0
soundfile==0.13.1
numpy>=2.4
pyloudnorm==0.2.0
soxr==1.0.0

# Step 2: Diarization (shared)
python-dotenv==1.1.0

# Step 2: AWS Transcribe mode
boto3>=1.38

# Step 2: GCP Speech-to-Text mode
google-cloud-speech>=2.30
google-cloud-storage>=2.19

# Step 10: Finetune (GCP VM orchestration)
google-cloud-compute>=1.22

# Step 2: WhisperX mode (heavy — install only if using local mode)
# Requires PyTorch: pip install torch torchaudio
# whisperx @ git+https://github.com/m-bain/whisperX.git
